{
  "title": "event.bookingAvailability.put",
  "x-internal": true,
  "type": "object",
  "description": "",
  "properties": {
    "type": {
      "type": "string",
      "minLength": 1,
      "enum": [
        "Available",
        "Unavailable"
      ],
      "description": "`Available` if the event still has tickets or places for reservation. `Unavailable` if there are no more tickets or places left."
    }
  },
  "required": [
    "type"
  ]
}