{
  "type": "array",
  "title": "place.labels",
  "description": "One or more labels that categorize the [place](../place.json). Compared to [terms](./terms.json), labels do not have a defined set of possible values or a hierarchy.\n\nLabels are allowed to be visible for end users on publication channels.\n\nSee [hiddenLabels](./hiddenLabels.json) for labels that should not be visible on publication channels.",
  "uniqueItems": true,
  "minItems": 1,
  "x-examples": {},
  "items": {
    "type": "string",
    "format": "uri",
    "minLength": 2,
    "maxLength": 50,
    "pattern": "^[A-Za-z0-9-_]+",
    "example": "label1"
  }
}