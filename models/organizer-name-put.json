{
  "title": "organizer-name-put",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "example": "publiq",
      "description": "The new name of the organizer (requires at least one character)",
      "minLength": 1,
      "pattern": "\\S"
    }
  },
  "required": [
    "name"
  ]
}
