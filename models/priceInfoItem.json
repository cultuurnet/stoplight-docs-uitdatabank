{
  "title": "priceInfoItem",
  "type": "object",
  "x-examples": {
    "Base tariff": {
      "category": "base",
      "price": 10.5,
      "priceCurrency": "EUR",
      "name": {
        "nl": "Basistarief",
        "fr": "Tarif de base",
        "en": "Base tariff",
        "de": "Basisrate"
      }
    },
    "Custom tariff": {
      "category": "tariff",
      "price": 6,
      "priceCurrency": "EUR",
      "name": {
        "nl": "Jongeren (<18j)",
        "en": "Youth (<18yo)"
      }
    }
  },
  "description": "A single priceInfo item.",
  "properties": {
    "category": {
      "type": "string",
      "enum": [
        "base",
        "tariff"
      ],
      "example": "base",
      "description": "Indicates whether this is the base tariff or a custom tariff.\n`base` for the base tariff, and `tariff` for custom/extra tariffs."
    },
    "price": {
      "type": "number",
      "format": "float",
      "minimum": 0,
      "example": 10.5
    },
    "priceCurrency": {
      "type": "string",
      "enum": [
        "EUR"
      ]
    },
    "name": {
      "$ref": "./priceInfoName.json"
    }
  },
  "required": [
    "category",
    "price",
    "priceCurrency"
  ]
}