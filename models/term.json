{
  "title": "term",
  "type": "object",
  "description": "A taxonomy term used to categorize events and places.\n\nAll events and places require exactly one term of the `eventtype` domain, and can optionally contain other terms.\n\nWhen reading events or places all properties will be available. When creating or updating events or places only the `id` is required to be included.",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique id of the term",
      "example": "0.50.4.0.0"
    },
    "label": {
      "type": "string",
      "description": "Human-readable label of the term. Currently only available in Dutch.",
      "example": "Concert"
    },
    "domain": {
      "type": "string",
      "example": "eventtype",
      "description": "The domain of the term.",
      "enum": [
        "eventtype",
        "theme"
      ]
    }
  },
  "required": [
    "id"
  ]
}